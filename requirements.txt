# 运行时 + 开发依赖（Python >=3.12）
# conda 用法：
#   conda create -n env_news_videos_wf python=3.12 && conda activate env_news_videos_wf
#   pip install -r requirements.txt

# ---- 运行时依赖 ----
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
httpx>=0.27
feedparser>=6.0
edge-tts>=6.1
openai>=2.0            # 订阅模式用 Responses API（client.responses.stream）
dashscope>=1.23.1
python-multipart>=0.0.9
jinja2>=3.1
python-docx>=1.1
pymupdf>=1.24

# ---- 开发依赖（pyproject 的 [dev] extra）----
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
ruff>=0.6

# ---- 采集功能可选依赖（按需安装；代码内懒加载，缺失则该源静默返回 0 篇）----
# DuckDuckGo 搜索源（启用 duckduckgo 采集器时需要）
duckduckgo-search
# 机器之心等无 RSS 的源走网页抓取：pip install "scrapling[all]" && scrapling install

# ---- 发布功能可选依赖（按需安装；pyproject 未列，代码内懒加载）----
# Bilibili 投稿
biliup
# YouTube 投稿（OAuth + 上传）
google-api-python-client
google-auth
google-auth-oauthlib
